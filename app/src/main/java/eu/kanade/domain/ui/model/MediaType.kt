package eu.kanade.domain.ui.model

/**
 * Content mode of the app. Drives accent theming, library/updates/history
 * filtering, verbs (Xem/Đọc) and progress units (tập/chương/trang).
 */
enum class MediaType {
    MANGA,
    ANIME,
    NOVEL,
}
